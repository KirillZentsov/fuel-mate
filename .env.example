# ─── Telegram ──────────────────────────────────────────────
# From @BotFather → /newbot
TELEGRAM_BOT_TOKEN=

# Private channel for admin notifications.
# Create channel, add bot as admin, get chat_id (negative number).
ADMIN_CHAT_ID=

# ─── Database (Supabase) ──────────────────────────────────
# From Supabase → Project Settings → Database → Connection string (URI)
# Use POOLED connection for serverless (GitHub Actions), DIRECT for bot.
# Bot (Railway):     direct  on port 5432
# ETL (GH Actions):  pooled  on port 6543
DATABASE_URL=

# ─── Government data source ───────────────────────────────
FUEL_PRICES_CSV_URL=https://www.fuel-finder.service.gov.uk/internal/v1.0.2/csv/get-latest-fuel-prices-csv

# ─── GitHub (for raw CSV archive) ─────────────────────────
# Personal access token with `repo` scope.
# Not needed locally — GitHub Actions injects this automatically.
GITHUB_TOKEN=
GITHUB_REPO=

# ─── Optional / development ───────────────────────────────
# LOG_LEVEL=DEBUG       # default INFO
# ETL_DRY_RUN=false     # if true, ETL fetches and parses but doesn't write to DB
